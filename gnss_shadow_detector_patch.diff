diff --git a/src/algorithms/tracking/gnuradio_blocks/gps_l1_ca_dll_pll_tracking_cc.cc b/src/algorithms/tracking/gnuradio_blocks/gps_l1_ca_dll_pll_tracking_cc.cc
index 1234567..abcdef0 100644
--- a/src/algorithms/tracking/gnuradio_blocks/gps_l1_ca_dll_pll_tracking_cc.cc
+++ b/src/algorithms/tracking/gnuradio_blocks/gps_l1_ca_dll_pll_tracking_cc.cc
@@ -456,6 +456,13 @@ int gps_l1_ca_dll_pll_tracking_cc::general_work(int noutput_items,
                     d_acc_carrier_phase_radians -= TWO_PI;
                 }
                 d_cn0_estimation = cn0_svn_estimator(..., d_Prompt_correlation);
+                // Publish shadow telemetry
+                pmt::pmt_t msg = pmt::make_dict();
+                pmt::dict_add(msg, pmt::mp("prn"), pmt::from_long(d_signal_prn));
+                pmt::dict_add(msg, pmt::mp("cn0"), pmt::from_float(d_cn0_estimation));
+                float early_late = d_Early_correlation - d_Late_correlation;
+                pmt::dict_add(msg, pmt::mp("early_late"), pmt::from_float(early_late));
+                this->message_port_pub(pmt::mp("telemetry"), msg);
             }
         }
     }
diff --git a/src/core/receiver/gnss_block_factory.cc b/src/core/receiver/gnss_block_factory.cc
index 9876543..deadbeef 100644
--- a/src/core/receiver/gnss_block_factory.cc
+++ b/src/core/receiver/gnss_block_factory.cc
@@ -55,6 +55,7 @@
 #include "pcps_acquisition_fine_doppler.h"
 #include "telemetry_decoder_gs.h"
 #include "channel_fsm.h"
+#include "shadow_detector.h"
 
 std::unique_ptr<GNSSBlockInterface> GNSSBlockFactory::GetBlock(
     const std::shared_ptr<InOutConfiguration>& config, ...) {
@@ -234,6 +235,15 @@ std::unique_ptr<GNSSBlockInterface> GNSSBlockFactory::GetBlock(
         return std::move(telemetry);
     }
 
+    if (implementation == "ShadowDetector") {
+        int station_id = config->property("ShadowDetector.station_id", 1);
+        std::string host = config->property("ShadowDetector.udp_host", "127.0.0.1");
+        int port = config->property("ShadowDetector.udp_port", 12345);
+        auto shadow = std::make_shared<ShadowDetector>(station_id, host, port);
+        // Connected via message port later in graph wiring
+        return std::move(shadow);
+    }
+
     return nullptr;
 }
